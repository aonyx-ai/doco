# Development

- Use `just` for all build/test/format operations (runs inside Flox)
- `just` — list all available commands
- `just test-rust` — run tests
- `just format-rust true` — format Rust code
- `just lint-rust` — lint Rust code
- `just pre-commit` — run all pre-commit checks
- Do NOT use `cargo` commands directly
